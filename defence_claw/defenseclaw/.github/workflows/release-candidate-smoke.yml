# Copyright 2026 Cisco Systems, Inc. and its affiliates
# SPDX-License-Identifier: Apache-2.0

name: Release candidate smoke

# release.yaml builds and signs one sealed candidate. This smoke workflow
# exercises those exact bytes in the same dispatch before publication.
on:
  workflow_call:
    inputs:
      candidate_artifact:
        description: Immutable Actions artifact containing release-candidate.tar.
        required: true
        type: string
      version:
        description: Canonical release version.
        required: true
        type: string
      commit:
        description: Exact reviewed source commit bound into the candidate.
        required: true
        type: string
      baselines:
        description: Authenticated published versions to upgrade from.
        required: true
        type: string

permissions:
  contents: read
  actions: read

env:
  UPGRADE_BASELINE_POLICY: ${{ github.workspace }}/release-candidate/effective-upgrade-baselines.json

jobs:
  posix-fresh-install:
    name: Fresh install exact candidate (${{ matrix.platform }})
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: ubuntu-latest
            platform: linux-amd64
            runner_arch: X64
          - runner: macos-15
            platform: darwin-arm64
            runner_arch: ARM64
    steps:
      - uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5.0.0
        with:
          ref: ${{ inputs.commit }}
          persist-credentials: false
      - uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b # v8.1.0
        with:
          python-version: "3.13"
      - uses: sigstore/cosign-installer@dc72c7d5c4d10cd6bcb8cf6e3fd625a9e5e537da # v3.7.0
        with:
          cosign-release: "v2.6.2"
      - uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4.3.0
        with:
          name: ${{ inputs.candidate_artifact }}
          path: candidate-transport
      - name: Restore exact candidate modes
        run: |
          set -euo pipefail
          python3 scripts/release_candidate.py unpack-transport \
            --archive candidate-transport/release-candidate.tar \
            --destination release-candidate
      - name: Verify and install exact signed bytes
        env:
          RELEASE_TAG: ${{ inputs.version }}
          RELEASE_COMMIT: ${{ inputs.commit }}
        run: |
          set -euo pipefail
          test "$RUNNER_ARCH" = "${{ matrix.runner_arch }}"
          python3 scripts/release_candidate.py verify \
            --root release-candidate --version "$RELEASE_TAG" --commit "$RELEASE_COMMIT"
          python3 scripts/verify-sigstore-blob.py \
            --certificate release-candidate/dist/checksums.txt.pem \
            --signature release-candidate/dist/checksums.txt.sig \
            --certificate-identity "https://github.com/$GITHUB_REPOSITORY/.github/workflows/release.yaml@refs/heads/main" \
            --certificate-oidc-issuer "https://token.actions.githubusercontent.com" \
            release-candidate/dist/checksums.txt
          bash scripts/test-fresh-install-release.sh \
            "$GITHUB_WORKSPACE/release-candidate/dist" "$RELEASE_TAG"

  posix-upgrade:
    name: Upgrade ${{ matrix.baseline }} → ${{ inputs.version }} (${{ matrix.platform.name }})
    runs-on: ${{ matrix.platform.runner }}
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        baseline: ${{ fromJSON(inputs.baselines) }}
        platform:
          - runner: ubuntu-latest
            name: linux-amd64
            runner_arch: X64
          - runner: macos-15
            name: darwin-arm64
            runner_arch: ARM64
    steps:
      - uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5.0.0
        with:
          ref: ${{ inputs.commit }}
          persist-credentials: false
      - uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b # v8.1.0
        with:
          python-version: "3.13"
      - uses: sigstore/cosign-installer@dc72c7d5c4d10cd6bcb8cf6e3fd625a9e5e537da # v3.7.0
        with:
          cosign-release: "v2.6.2"
      - uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4.3.0
        with:
          name: ${{ inputs.candidate_artifact }}
          path: candidate-transport
      - name: Restore exact candidate modes
        run: |
          set -euo pipefail
          python3 scripts/release_candidate.py unpack-transport \
            --archive candidate-transport/release-candidate.tar \
            --destination release-candidate
      - name: Exercise authenticated upgrade baseline
        env:
          BASELINE: ${{ matrix.baseline }}
          SELECTED_BASELINES: ${{ inputs.baselines }}
          RELEASE_TAG: ${{ inputs.version }}
          RELEASE_COMMIT: ${{ inputs.commit }}
          UPGRADE_SMOKE_SKIP_SOURCE_CONTRACTS: "1"
        run: |
          set -euo pipefail
          test "$RUNNER_ARCH" = "${{ matrix.platform.runner_arch }}"
          python3 scripts/release_candidate.py verify \
            --root release-candidate --version "$RELEASE_TAG" --commit "$RELEASE_COMMIT"
          python3 scripts/verify-sigstore-blob.py \
            --certificate release-candidate/dist/checksums.txt.pem \
            --signature release-candidate/dist/checksums.txt.sig \
            --certificate-identity "https://github.com/$GITHUB_REPOSITORY/.github/workflows/release.yaml@refs/heads/main" \
            --certificate-oidc-issuer "https://token.actions.githubusercontent.com" \
            release-candidate/dist/checksums.txt
          # Derive special validation roles from the signed candidate's
          # effective policy and manifest. The caller-provided matrix is only
          # accepted when it contains both authenticated anchors.
          lane_role="$(python3 scripts/select-release-validation-lane.py \
            --policy "$UPGRADE_BASELINE_POLICY" \
            --manifest release-candidate/dist/upgrade-manifest.json \
            --selected-json "$SELECTED_BASELINES" \
            --baseline "$BASELINE")"
          baseline_dependencies=published
          UPGRADE_SMOKE_FIELD_RECOVERY_CASES=0
          case "$lane_role" in
            bridge-dependency-drift)
              # The authenticated bridge boundary deliberately starts with
              # dependency drift. The resolver must normalize it before the
              # first native-v8 cut.
              baseline_dependencies=target
              ;;
            field-recovery)
              # The derived immutable anchor exercises both the exact clean
              # missing-cursor state and corrupt local audit recovery.
              UPGRADE_SMOKE_FIELD_RECOVERY_CASES=1
              ;;
            published) ;;
            *) echo "invalid release validation lane: $lane_role" >&2; exit 1 ;;
          esac
          export UPGRADE_SMOKE_FIELD_RECOVERY_CASES
          smoke_baselines="$BASELINE"
          if [[ "$BASELINE" == "0.8.5" ]]; then
            # Keep the selected seven-lane policy compact while proving the
            # exact public source that exercises the longest supported bridge.
            smoke_baselines="${BASELINE},0.8.1"
          fi
          bash scripts/test-upgrade-protocol-release.sh \
            --from-versions "$smoke_baselines" \
            --target-version "$RELEASE_TAG" \
            --release-dir "$GITHUB_WORKSPACE/release-candidate/dist" \
            --baseline-mode seed \
            --baseline-dependencies "$baseline_dependencies" \
            --success-path-only \
            --health-timeout 60

  macos-intel-refusal:
    name: Reject unsupported Intel macOS exact candidate (${{ matrix.surface }})
    runs-on: macos-15-intel
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        surface:
          - fresh-install
          - upgrade
    steps:
      - uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5.0.0
        with:
          ref: ${{ inputs.commit }}
          persist-credentials: false
      - uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b # v8.1.0
        with:
          python-version: "3.13"
      - uses: sigstore/cosign-installer@dc72c7d5c4d10cd6bcb8cf6e3fd625a9e5e537da # v3.7.0
        with:
          cosign-release: "v2.6.2"
      - uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4.3.0
        with:
          name: ${{ inputs.candidate_artifact }}
          path: candidate-transport
      - name: Restore exact candidate modes
        run: |
          set -euo pipefail
          test "$RUNNER_ARCH" = "X64"
          python3 scripts/release_candidate.py unpack-transport \
            --archive candidate-transport/release-candidate.tar \
            --destination release-candidate
      - name: Authenticate candidate and prove pre-effect refusal
        env:
          RELEASE_TAG: ${{ inputs.version }}
          RELEASE_COMMIT: ${{ inputs.commit }}
          REFUSAL_SURFACE: ${{ matrix.surface }}
        run: |
          set -euo pipefail
          python3 scripts/release_candidate.py verify \
            --root release-candidate --version "$RELEASE_TAG" --commit "$RELEASE_COMMIT"
          python3 scripts/verify-sigstore-blob.py \
            --certificate release-candidate/dist/checksums.txt.pem \
            --signature release-candidate/dist/checksums.txt.sig \
            --certificate-identity "https://github.com/$GITHUB_REPOSITORY/.github/workflows/release.yaml@refs/heads/main" \
            --certificate-oidc-issuer "https://token.actions.githubusercontent.com" \
            release-candidate/dist/checksums.txt
          bash scripts/test-upgrade-macos-intel-refusal.sh \
            --surface "$REFUSAL_SURFACE" \
            --release-dir "$GITHUB_WORKSPACE/release-candidate/dist" \
            --version "$RELEASE_TAG"

  windows-fresh-install:
    name: Fresh install exact candidate (windows-amd64)
    runs-on: windows-latest
    timeout-minutes: 45
    defaults:
      run:
        shell: pwsh
    steps:
      - uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5.0.0
        with:
          ref: ${{ inputs.commit }}
          persist-credentials: false
      - uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b # v8.1.0
        with:
          python-version: "3.13"
      - uses: sigstore/cosign-installer@dc72c7d5c4d10cd6bcb8cf6e3fd625a9e5e537da # v3.7.0
        with:
          cosign-release: "v2.6.2"
      - uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4.3.0
        with:
          name: ${{ inputs.candidate_artifact }}
          path: candidate-transport
      - name: Restore exact candidate modes
        run: |
          $ErrorActionPreference = 'Stop'
          python scripts/release_candidate.py unpack-transport `
            --archive candidate-transport/release-candidate.tar `
            --destination release-candidate
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
      - name: Verify and exercise install.ps1
        env:
          RELEASE_TAG: ${{ inputs.version }}
          RELEASE_COMMIT: ${{ inputs.commit }}
        run: |
          $ErrorActionPreference = 'Stop'
          python scripts/release_candidate.py verify `
            --root release-candidate --version "$env:RELEASE_TAG" --commit "$env:RELEASE_COMMIT"
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          python scripts/verify-sigstore-blob.py `
            --certificate release-candidate/dist/checksums.txt.pem `
            --signature release-candidate/dist/checksums.txt.sig `
            --certificate-identity "https://github.com/$env:GITHUB_REPOSITORY/.github/workflows/release.yaml@refs/heads/main" `
            --certificate-oidc-issuer "https://token.actions.githubusercontent.com" `
            release-candidate/dist/checksums.txt
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          ./scripts/test-fresh-install-release-windows.ps1 `
            -ReleaseDir "$env:GITHUB_WORKSPACE/release-candidate/dist" `
            -TargetVersion "$env:RELEASE_TAG" `
            -UninstallContract deferred `
            -DiagnosticsRoot (Join-Path $env:RUNNER_TEMP 'defenseclaw-release-bootstrap-diagnostics')
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
      - name: Upload Windows bootstrap diagnostics on failure
        if: ${{ failure() || cancelled() }}
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: release-windows-bootstrap-diagnostics-${{ github.run_id }}-${{ github.run_attempt }}
          path: ${{ runner.temp }}/defenseclaw-release-bootstrap-diagnostics/**
          if-no-files-found: warn
          retention-days: 7
