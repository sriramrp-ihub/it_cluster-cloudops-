[DefenseClaw - Recent Audit Events]
description = Recent DefenseClaw audit activity for local investigation.
search = `defenseclaw_audit_base` | table timestamp run_id session_id action severity actor target details component status | sort - timestamp | head 50
is_visible = 1
disabled = 0
is_scheduled = 0

[DefenseClaw - High Severity Activity]
description = DefenseClaw audit events that indicate elevated severity or control activity.
search = `defenseclaw_notable_audit` | table timestamp run_id session_id risk_bucket action severity actor target details | sort - timestamp | head 50
is_visible = 1
disabled = 0
is_scheduled = 0

[DefenseClaw - Top Actions]
description = Most common DefenseClaw actions in the last 24 hours.
search = `defenseclaw_audit_base` | stats count by action | sort - count | head 20
is_visible = 1
disabled = 0
is_scheduled = 0

[DefenseClaw - Recent Run And Session Inventory]
description = Most recent DefenseClaw run and session pairs with associated actions and severities.
search = `defenseclaw_run_inventory` | table run_id session_id count first_seen last_seen actions severities actors targets | head 20
is_visible = 1
disabled = 0
is_scheduled = 0

[DefenseClaw - Recent Deny And Enforcement Activity]
description = Recent deny, block, quarantine, and enforcement actions for security review.
search = `defenseclaw_control_actions` | table timestamp run_id session_id action severity actor target details | sort - timestamp | head 50
is_visible = 1
disabled = 0
is_scheduled = 0

[OpenClaw - Recent Gateway Errors]
description = Recent structured gateway errors for local troubleshooting.
search = `openclaw_gateway_errors` | table timestamp level subsystem run_id session_id status error_code request_id message | sort - timestamp | head 50
is_visible = 1
disabled = 0
is_scheduled = 0

[OpenClaw - Gateway Log Volume By Subsystem]
description = Structured gateway-log volume grouped by subsystem and level.
search = `openclaw_gateway_logs_base` | stats count by subsystem level | sort - count | head 30
is_visible = 1
disabled = 0
is_scheduled = 0

[OpenClaw - Recent Runtime Health Symptoms]
description = Recent log-derived runtime health symptoms from gateway logs.
search = `openclaw_gateway_runtime_symptoms` | table timestamp level subsystem status error_code run_id session_id message | sort - timestamp | head 50
is_visible = 1
disabled = 0
is_scheduled = 0

[OpenClaw - Repeated Error Signatures]
description = Repeated gateway error signatures grouped for troubleshooting.
search = `openclaw_gateway_error_signatures` | stats count latest(timestamp) as latest_seen values(message) as messages values(run_id) as run_ids values(session_id) as session_ids by subsystem error_signature | sort - count | head 30
is_visible = 1
disabled = 0
is_scheduled = 0

[OpenClaw - Recent Run And Session Correlated Gateway Logs]
description = Structured gateway logs that contain run or session identifiers.
search = `openclaw_gateway_logs_base` | search run_id!="(unknown)" OR session_id!="(unknown)" | table timestamp level subsystem run_id session_id request_id status message | sort - timestamp | head 50
is_visible = 1
disabled = 0
is_scheduled = 0

[OpenClaw - Recent Diagnostics Events]
description = Recent structured diagnostics events for queue, session, and processing analytics.
search = `openclaw_diagnostics_base` | table timestamp event_name lane session_id session_key run_id outcome reason wait_ms queue_depth duration_ms attempt | sort - timestamp | head 50
is_visible = 1
disabled = 0
is_scheduled = 0

[OpenClaw - Queue Lane Activity]
description = Recent queue lane activity grouped by lane and outcome.
search = `openclaw_queue_lane_events` | stats count avg(wait_ms) as avg_wait_ms max(wait_ms) as max_wait_ms by lane outcome | sort - count | head 30
is_visible = 1
disabled = 0
is_scheduled = 0

[OpenClaw - Recent Stuck Sessions]
description = Recent diagnostics events indicating stuck sessions.
search = `openclaw_stuck_sessions` | table timestamp session_id session_key run_id lane outcome reason wait_ms | sort - timestamp | head 50
is_visible = 1
disabled = 0
is_scheduled = 0

[OpenClaw - Recent Run Attempts And Retries]
description = Recent run attempts, retries, and escalation-related diagnostics events.
search = `openclaw_run_attempt_events` | table timestamp run_id session_id session_key attempt outcome reason duration_ms | sort - timestamp | head 50
is_visible = 1
disabled = 0
is_scheduled = 0

[OpenClaw - Recent Message Processing Failures]
description = Recent failed message processing diagnostics events.
search = `openclaw_message_processing_failures` | table timestamp session_id session_key run_id outcome reason duration_ms provider model | sort - timestamp | head 50
is_visible = 1
disabled = 0
is_scheduled = 0

[OpenClaw - Recent Session State Transitions]
description = Recent session state transitions from diagnostics events.
search = `openclaw_session_state_events` | table timestamp session_id session_key run_id outcome reason status | sort - timestamp | head 50
is_visible = 1
disabled = 0
is_scheduled = 0

[OpenClaw - Recent Model Usage Metrics]
description = Recent token, cost, and related usage metrics for provider and model analysis.
search = `openclaw_metrics_base` | table timestamp metric_name metric_value unit provider model run_id session_id session_key | sort - timestamp | head 50
is_visible = 1
disabled = 0
is_scheduled = 0

[OpenClaw - Cost By Provider And Model]
description = Cost metrics grouped by provider and model from event-indexed OTEL metrics.
search = `openclaw_cost_metrics` | stats sum(metric_value) as total_cost_usd by provider model | sort - total_cost_usd | head 30
is_visible = 1
disabled = 0
is_scheduled = 0

[OpenClaw - Run Duration And Queue Wait]
description = Duration and queue-wait metrics for recent providers and models.
search = `openclaw_duration_metrics` | stats avg(metric_value) as avg_metric_value max(metric_value) as max_metric_value by metric_name provider model | sort - max_metric_value | head 30
is_visible = 1
disabled = 0
is_scheduled = 0

[OpenClaw - Recent Trace Spans By Span Name]
description = Recent OTEL trace spans grouped by span name for run and session investigation.
search = `openclaw_usage_traces` | table timestamp span_name trace_id span_id run_id session_id duration_ms provider model status | sort - timestamp | head 50
is_visible = 1
disabled = 0
is_scheduled = 0

[OpenClaw - Recent High Cost Or Slow Runs]
description = Recent runs that look expensive, slow, or queue-pressured from event-indexed metrics.
search = `openclaw_high_cost_or_slow_runs` | table run_id session_id session_key cost_usd run_duration_ms queue_wait_ms providers models | head 30
is_visible = 1
disabled = 0
is_scheduled = 0

[OpenClaw - Recent Run And Session Correlated Metrics And Traces]
description = Recent run and session correlated metrics and traces for guided investigation.
search = `openclaw_metrics_base` | eval signal_name=metric_name, signal_family="metric" | fields timestamp run_id session_id session_key provider model signal_family signal_name metric_value | append [ search `openclaw_usage_traces` | eval signal_name=span_name, signal_family="trace" | fields timestamp run_id session_id session_key provider model signal_family signal_name duration_ms ] | search run_id!="(unknown)" OR session_id!="(unknown)" | sort - timestamp | head 50
is_visible = 1
disabled = 0
is_scheduled = 0

[DefenseClaw Internal - Daily Product Telemetry Usage Summary]
description = Internal bounded daily product-telemetry summary for DefenseClaw local mode.
search = | makeresults | fields - _time | appendcols [ search index=defenseclaw_local source=defenseclaw sourcetype="defenseclaw:json" | stats count as audit_events_24h ] | appendcols [ search index=defenseclaw_local sourcetype="openclaw:gateway:json" | stats count as gateway_events_24h ] | appendcols [ search index=defenseclaw_local sourcetype="openclaw:diagnostics:json" | stats count as diagnostics_events_24h ] | appendcols [ search index=defenseclaw_local sourcetype="otel:log" | stats count as log_events_24h ] | appendcols [ search index=defenseclaw_local sourcetype="otel:metric" | stats count as metric_events_24h ] | appendcols [ search index=defenseclaw_local sourcetype="otel:trace" | stats count as trace_events_24h ] | appendcols [ | rest /servicesNS/nobody/defenseclaw_local_mode/saved/searches splunk_server=local | search title="DefenseClaw Alert *" OR title="OpenClaw Alert *" OR title="DefenseClaw Security Ops -*" | stats count(eval(disabled="0")) as alerts_enabled_count ] | eval alerts_enabled_count=coalesce(alerts_enabled_count, 0) | eval signal_families_seen_24h=mvappend(if(coalesce(audit_events_24h, 0)>0, "audit", null()), if(coalesce(gateway_events_24h, 0)>0, "gateway_log", null()), if(coalesce(diagnostics_events_24h, 0)>0, "diagnostics", null()), if(coalesce(log_events_24h, 0)>0, "log", null()), if(coalesce(metric_events_24h, 0)>0, "metric", null()), if(coalesce(trace_events_24h, 0)>0, "trace", null())) | eval signal_families_seen_24h=if(mvcount(signal_families_seen_24h)=0, "", mvjoin(signal_families_seen_24h, ",")) | fields alerts_enabled_count signal_families_seen_24h
is_visible = 0
disabled = 1
is_scheduled = 1
cron_schedule = 17 3 * * *
dispatch.earliest_time = -24h@h
dispatch.latest_time = now
alert_type = number of events
alert_comparator = greater than
alert_threshold = 0
action.email = 0
action.script = 1
action.script.filename = emit_product_telemetry_usage_summary.py
action.webhook = 0

[DefenseClaw Security Ops - Repeated Deny Activity]
description = Disabled-by-default security-operations detection for repeated deny activity on a correlated run or session.
search = `dcso_detection_repeated_deny_activity`
is_visible = 1
disabled = 1
is_scheduled = 1
cron_schedule = */5 * * * *
dispatch.earliest_time = -15m@m
dispatch.latest_time = now
alert_type = number of events
alert_comparator = greater than
alert_threshold = 0
action.email = 0
action.script = 0
action.webhook = 0

[DefenseClaw Security Ops - Dangerous Command Or Tool Denied]
description = Disabled-by-default security-operations detection for denied dangerous command or tool evidence.
search = `dcso_detection_dangerous_command_or_tool_denied`
is_visible = 1
disabled = 1
is_scheduled = 1
cron_schedule = */5 * * * *
dispatch.earliest_time = -15m@m
dispatch.latest_time = now
alert_type = number of events
alert_comparator = greater than
alert_threshold = 0
action.email = 0
action.script = 0
action.webhook = 0

[DefenseClaw Security Ops - Policy Bypass Or Retry After Denial]
description = Disabled-by-default security-operations detection for retry-after-denial or bypass-like behavior in the same investigation.
search = `dcso_detection_policy_bypass_or_retry_after_denial`
is_visible = 1
disabled = 1
is_scheduled = 1
cron_schedule = */5 * * * *
dispatch.earliest_time = -15m@m
dispatch.latest_time = now
alert_type = number of events
alert_comparator = greater than
alert_threshold = 0
action.email = 0
action.script = 0
action.webhook = 0

[DefenseClaw Security Ops - Stuck Or Retry-Storm Session]
description = Disabled-by-default security-operations detection for stuck sessions or retry storms.
search = `dcso_detection_stuck_or_retry_storm_session`
is_visible = 1
disabled = 1
is_scheduled = 1
cron_schedule = */5 * * * *
dispatch.earliest_time = -15m@m
dispatch.latest_time = now
alert_type = number of events
alert_comparator = greater than
alert_threshold = 0
action.email = 0
action.script = 0
action.webhook = 0

[DefenseClaw Security Ops - Runtime Error Burst Around Risky Run]
description = Disabled-by-default security-operations detection for repeated runtime errors around a risky run or session.
search = `dcso_detection_runtime_error_burst_around_risky_run`
is_visible = 1
disabled = 1
is_scheduled = 1
cron_schedule = */5 * * * *
dispatch.earliest_time = -15m@m
dispatch.latest_time = now
alert_type = number of events
alert_comparator = greater than
alert_threshold = 0
action.email = 0
action.script = 0
action.webhook = 0

[DefenseClaw Security Ops - High-Cost Or Long-Duration Risky Run]
description = Disabled-by-default security-operations detection for expensive or slow risky investigations.
search = `dcso_detection_high_cost_or_long_duration_risky_run`
is_visible = 1
disabled = 1
is_scheduled = 1
cron_schedule = */5 * * * *
dispatch.earliest_time = -15m@m
dispatch.latest_time = now
alert_type = number of events
alert_comparator = greater than
alert_threshold = 0
action.email = 0
action.script = 0
action.webhook = 0

[DefenseClaw Security Ops - Latest Critical Investigations]
description = Latest critical investigations for the command-center critical queue.
search = `dcso_latest_critical_investigations`
is_visible = 1
disabled = 0
is_scheduled = 0

[DefenseClaw Security Ops - Top Risky Runs Sessions]
description = Ranked risky and critical investigations for the command-center priority list.
search = `dcso_top_risky_runs_sessions`
is_visible = 1
disabled = 0
is_scheduled = 0

[DefenseClaw Alert - Repeated Deny Activity]
description = Disabled-by-default local alert for repeated deny or block activity in the same run or session.
search = search `defenseclaw_repeated_deny_activity`
is_visible = 1
disabled = 1
is_scheduled = 1
cron_schedule = */5 * * * *
dispatch.earliest_time = -15m@m
dispatch.latest_time = now
alert_type = number of events
alert_comparator = greater than
alert_threshold = 0
action.email = 0
action.script = 0
action.webhook = 0

[DefenseClaw Alert - Enforcement Action Detected]
description = Disabled-by-default local alert for enforcement or quarantine actions that need analyst follow-up.
search = search `defenseclaw_enforcement_action_detected`
is_visible = 1
disabled = 1
is_scheduled = 1
cron_schedule = */5 * * * *
dispatch.earliest_time = -15m@m
dispatch.latest_time = now
alert_type = number of events
alert_comparator = greater than
alert_threshold = 0
action.email = 0
action.script = 0
action.webhook = 0

[DefenseClaw Alert - High Severity Audit Burst]
description = Disabled-by-default local alert for repeated ERROR or CRITICAL audit activity in the same run or session.
search = search `defenseclaw_high_severity_burst`
is_visible = 1
disabled = 1
is_scheduled = 1
cron_schedule = */5 * * * *
dispatch.earliest_time = -15m@m
dispatch.latest_time = now
alert_type = number of events
alert_comparator = greater than
alert_threshold = 0
action.email = 0
action.script = 0
action.webhook = 0

[OpenClaw Alert - Repeated Gateway Error Signature]
description = Disabled-by-default local alert for repeated structured gateway error signatures.
search = search `openclaw_repeated_gateway_error_signature`
is_visible = 1
disabled = 1
is_scheduled = 1
cron_schedule = */5 * * * *
dispatch.earliest_time = -15m@m
dispatch.latest_time = now
alert_type = number of events
alert_comparator = greater than
alert_threshold = 0
action.email = 0
action.script = 0
action.webhook = 0

[OpenClaw Alert - Queue Pressure Sustained]
description = Disabled-by-default local alert for repeated queue-depth or queue-wait pressure across diagnostics and metrics.
search = search `openclaw_queue_pressure_sustained`
is_visible = 1
disabled = 1
is_scheduled = 1
cron_schedule = */5 * * * *
dispatch.earliest_time = -15m@m
dispatch.latest_time = now
alert_type = number of events
alert_comparator = greater than
alert_threshold = 0
action.email = 0
action.script = 0
action.webhook = 0

[OpenClaw Alert - Retry Storm Or Stuck Session]
description = Disabled-by-default local alert for stuck sessions or retry escalation patterns in diagnostics events.
search = search `openclaw_retry_or_stuck_sessions`
is_visible = 1
disabled = 1
is_scheduled = 1
cron_schedule = */5 * * * *
dispatch.earliest_time = -15m@m
dispatch.latest_time = now
alert_type = number of events
alert_comparator = greater than
alert_threshold = 0
action.email = 0
action.script = 0
action.webhook = 0

[OpenClaw Alert - High Cost Or Slow Run]
description = Disabled-by-default local alert for high-cost or slow runs from the event-indexed metrics contract.
search = search `openclaw_high_cost_or_slow_run_alert`
is_visible = 1
disabled = 1
is_scheduled = 1
cron_schedule = */5 * * * *
dispatch.earliest_time = -15m@m
dispatch.latest_time = now
alert_type = number of events
alert_comparator = greater than
alert_threshold = 0
action.email = 0
action.script = 0
action.webhook = 0

[DefenseClaw Gateway - Circuit Breaker Open]
description = Disabled-by-default alert that fires when the Splunk HEC sink circuit breaker transitions to open state. Emitted by the PR#1 circuit breaker via CircuitState() structured log events.
search = `defenseclaw_circuit_breaker_events` | stats latest(circuit_state_num) as circuit_state_num | where circuit_state_num=2
is_visible = 1
disabled = 1
is_scheduled = 1
cron_schedule = */2 * * * *
dispatch.earliest_time = -5m@m
dispatch.latest_time = now
alert_type = number of events
alert_comparator = greater than
alert_threshold = 0
action.email = 0
action.script = 0
action.webhook = 0

[DefenseClaw Gateway - Sink Failure Burst]
description = Disabled-by-default alert for repeated audit sink failures within a short window, indicating a persistent HEC delivery problem.
search = `defenseclaw_sink_failures` | stats count as failure_count | where failure_count>=3
is_visible = 1
disabled = 1
is_scheduled = 1
cron_schedule = */5 * * * *
dispatch.earliest_time = -10m@m
dispatch.latest_time = now
alert_type = number of events
alert_comparator = greater than
alert_threshold = 0
action.email = 0
action.script = 0
action.webhook = 0

[DefenseClaw Gateway - Schema Violation Detected]
description = Disabled-by-default alert for any schema violations in the gateway log writer. Any non-zero count is a contract bug.
search = `defenseclaw_schema_violation_events` | stats count as violation_count | where violation_count>=1
is_visible = 1
disabled = 1
is_scheduled = 1
cron_schedule = */5 * * * *
dispatch.earliest_time = -15m@m
dispatch.latest_time = now
alert_type = number of events
alert_comparator = greater than
alert_threshold = 0
action.email = 0
action.script = 0
action.webhook = 0

[DefenseClaw Gateway - Recent Verdict Activity]
description = Recent gateway verdict events for operational review.
search = `defenseclaw_gateway_verdicts_base` | table timestamp verdict_action action severity run_id session_id component details | sort - timestamp | head 50
is_visible = 1
disabled = 0
is_scheduled = 0

[DefenseClaw Gateway - Circuit Breaker History]
description = Full circuit breaker state change history for the Splunk HEC sink.
search = `defenseclaw_circuit_breaker_events` | table timestamp component circuit_state circuit_state_num action severity details | sort - timestamp | head 100
is_visible = 1
disabled = 0
is_scheduled = 0
