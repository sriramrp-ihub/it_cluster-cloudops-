# Recording rules — pre-aggregated series so dashboards stay snappy and
# alerts have stable building blocks.
#
# Naming convention: <level>:<metric>:<agg>_<window>
#   level = job | service | route | scanner | sink | webhook
#   agg   = rate5m | ratio5m | p95_5m | ...
groups:
  - name: defenseclaw.guardrail
    interval: 30s
    rules:
      # Canonical source family: metric.defenseclaw.guardrail.evaluations.
      # The labels below are the frozen local-observability-v1 projections of
      # defenseclaw.guardrail.effective_action, defenseclaw.connector.source,
      # and defenseclaw.metric.guardrail.scanner.
      - record: service:defenseclaw_guardrail_evaluations:rate5m
        expr: sum(rate(defenseclaw_guardrail_evaluations_total[5m]))
      - record: service:defenseclaw_guardrail_evaluations_by_action:rate5m
        expr: sum by (guardrail_action_taken) (rate(defenseclaw_guardrail_evaluations_total[5m]))
      - record: service:defenseclaw_guardrail_evaluations_by_connector:rate5m
        expr: sum by (guardrail_connector) (rate(defenseclaw_guardrail_evaluations_total[5m]))
      - record: service:defenseclaw_guardrail_evaluations_by_scanner:rate5m
        expr: sum by (guardrail_scanner) (rate(defenseclaw_guardrail_evaluations_total[5m]))
      - record: service:defenseclaw_guardrail_block_ratio:5m
        expr: |
          sum(rate(defenseclaw_guardrail_evaluations_total{guardrail_action_taken="block"}[5m]))
          /
          clamp_min(sum(rate(defenseclaw_guardrail_evaluations_total[5m])), 0.0001)

  - name: defenseclaw.http
    interval: 30s
    rules:
      - record: service:defenseclaw_http_requests:rate5m
        expr: sum(rate(defenseclaw_http_request_count_total[5m]))
      - record: service:defenseclaw_http_requests_5xx_ratio:5m
        expr: |
          sum(rate(defenseclaw_http_request_count_total{http_status_code=~"5.."}[5m]))
          /
          clamp_min(sum(rate(defenseclaw_http_request_count_total[5m])), 0.0001)
      - record: route:defenseclaw_http_duration:p95_5m
        expr: |
          histogram_quantile(0.95,
            sum by (le, http_route) (rate(defenseclaw_http_request_duration_milliseconds_bucket[5m])))

  - name: defenseclaw.scanner
    interval: 30s
    rules:
      - record: scanner:defenseclaw_scan:rate5m
        expr: sum by (scanner) (rate(defenseclaw_scan_count_total[5m]))
      - record: scanner:defenseclaw_scan_errors:rate5m
        expr: sum by (scanner) (rate(defenseclaw_scan_errors_total[5m]))
      - record: scanner:defenseclaw_scan_duration:p95_5m
        expr: |
          histogram_quantile(0.95,
            sum by (le, scanner) (rate(defenseclaw_scan_duration_milliseconds_bucket[5m])))
      - record: scanner:defenseclaw_findings_by_severity:rate5m
        expr: sum by (scanner, severity) (rate(defenseclaw_scan_findings_total[5m]))

  - name: defenseclaw.slo.recording
    interval: 30s
    rules:
      # ratio_5m is defined as "good events / total events" and is
      # clamped to 1.0 when there is no traffic (avoids NaN rendering
      # "no data" panels during quiet windows). See alerts.yml for the
      # traffic-floor gate that keeps these from paging on idle systems.
      - record: slo:defenseclaw_block_latency:ratio_5m
        expr: |
          (
            sum(rate(defenseclaw_slo_block_latency_milliseconds_bucket{le="2000"}[5m]))
            /
            clamp_min(sum(rate(defenseclaw_slo_block_latency_milliseconds_count[5m])), 0.0001)
          )
          or on() vector(1)
      # 30-day multi-window burn rate building blocks.
      - record: slo:defenseclaw_block_latency:errors_1h
        expr: |
          sum(rate(defenseclaw_slo_block_latency_milliseconds_bucket{le="+Inf"}[1h]))
          -
          sum(rate(defenseclaw_slo_block_latency_milliseconds_bucket{le="2000"}[1h]))
      - record: slo:defenseclaw_block_latency:total_1h
        expr: sum(rate(defenseclaw_slo_block_latency_milliseconds_count[1h]))
      - record: slo:defenseclaw_block_latency:error_ratio_1h
        expr: |
          slo:defenseclaw_block_latency:errors_1h
          /
          clamp_min(slo:defenseclaw_block_latency:total_1h, 0.0001)

  - name: defenseclaw.sinks
    interval: 30s
    rules:
      # defenseclaw.audit.sink.failures uses canonical
      # defenseclaw.metric.sink.{kind,name,reason} attributes and the generated
      # local labels sink_kind / sink_name / sink_reason. Do not change this
      # group-by without also updating alerts.yml::DefenseClawAuditSinkFailures.
      - record: sink:defenseclaw_audit_sink_failures:rate5m
        expr: sum by (sink_kind, sink_name) (rate(defenseclaw_audit_sink_failures_total[5m]))
      # defenseclaw.audit.sink.batches.* uses the frozen
      # local-observability-v1 `sink` compatibility label. Keep `sink` here
      # until the declared alias-removal window closes.
      #
      # Drop-ratio is semantically undefined on a silent sink: a
      # single dropped batch with zero deliveries within the lookback
      # would compute as 100% drop, which reads as a catastrophic
      # outage on panels even though the sink is simply quiet. We gate
      # on total traffic > 0 so idle sinks drop out of the result
      # (Grafana renders "no data" instead of a phantom red tile).
      - record: sink:defenseclaw_audit_sink_drop_ratio:5m
        expr: |
          (
            sum by (sink) (rate(defenseclaw_audit_sink_batches_dropped_total[5m]))
            /
            clamp_min(
              sum by (sink) (
                rate(defenseclaw_audit_sink_batches_delivered_total[5m])
                + rate(defenseclaw_audit_sink_batches_dropped_total[5m])
              ),
              0.0001)
          )
          and
          sum by (sink) (
            rate(defenseclaw_audit_sink_batches_delivered_total[5m])
            + rate(defenseclaw_audit_sink_batches_dropped_total[5m])
          ) > 0

  - name: defenseclaw.exporters
    interval: 30s
    rules:
      # Canonical source family:
      # metric.defenseclaw.telemetry.exporter.last_export_ts. Deliberately
      # group by its generated exporter label and do not pin a deployment job.
      - record: exporter:defenseclaw_otlp_freshness:seconds
        expr: time() - max by (exporter) (defenseclaw_telemetry_exporter_last_export_ts_seconds)

  # Observability-pipeline self-monitoring. Loki rejects log
  # entries that exceed `max_line_size` or `max_structured_metadata_size`,
  # which manifests as missing audit events on dashboards rather than
  # an obvious failure. We pre-aggregate the per-reason discard rate
  # plus a ratio against accepted lines so the
  # DefenseClawLokiIngestOverflow alert (alerts.yml::
  # defenseclaw.observability_pipeline) can fire on either an absolute
  # rate threshold or a fraction-of-traffic threshold.
  - name: defenseclaw.observability_pipeline
    interval: 30s
    rules:
      - record: pipeline:loki_ingest_overflow:rate5m
        expr: |
          sum by (reason) (
            rate(loki_discarded_samples_total{reason=~"line_too_long|structured_metadata_too_big"}[5m])
          )
      - record: pipeline:loki_ingest_overflow:ratio5m
        expr: |
          (
            sum(rate(loki_discarded_samples_total{reason=~"line_too_long|structured_metadata_too_big"}[5m]))
            /
            clamp_min(sum(rate(loki_distributor_lines_received_total[5m])), 0.0001)
          )
          and
          sum(rate(loki_distributor_lines_received_total[5m])) > 0

  # Connector ingest (codex / claudecode native telemetry posted to
  # /v1/logs, /v1/metrics, /v1/traces, /api/v1/codex/notify).
  # Pre-aggregated so the connector dashboard stays snappy even when
  # an operator zooms out to 30 days of data. Group-by labels are
  # signal (logs|metrics|traces) and source (codex|claudecode|unknown);
  # cardinality is bounded by RecordOTelIngest's allow-list.
  - name: defenseclaw.connectors
    interval: 30s
    rules:
      - record: connector:defenseclaw_otel_ingest_requests:rate5m
        expr: sum by (signal, source, result) (rate(defenseclaw_otel_ingest_requests_total[5m]))
      - record: connector:defenseclaw_otel_ingest_records:rate5m
        expr: sum by (signal, source) (rate(defenseclaw_otel_ingest_records_total[5m]))
      - record: connector:defenseclaw_otel_ingest_bytes:rate5m
        expr: sum by (signal, source) (rate(defenseclaw_otel_ingest_bytes_total[5m]))
      - record: connector:defenseclaw_otel_ingest_malformed:ratio_5m
        expr: |
          (
            sum by (signal, source) (rate(defenseclaw_otel_ingest_malformed_total[5m]))
            /
            clamp_min(sum by (signal, source) (rate(defenseclaw_otel_ingest_requests_total[5m])), 0.0001)
          )
      # silence_seconds is "wall-clock seconds since the last accepted
      # batch". The ConnectorTelemetrySilent alert pages on this
      # exceeding 600 (10 minutes) for any connector that has emitted
      # at least one batch since the gateway started — so a never-used
      # connector doesn't fire spuriously (last_seen is absent → no
      # series, alert returns empty, no page).
      - record: connector:defenseclaw_otel_ingest_silence:seconds
        expr: time() - max by (signal, source) (defenseclaw_otel_ingest_last_seen_ts_seconds)
      - record: connector:defenseclaw_codex_notify:rate5m
        expr: sum by (type, status, result) (rate(defenseclaw_codex_notify_total[5m]))
      # Hook-vs-OTel rates exposed separately so the Connector
      # Telemetry dashboard can render both side-by-side and operators
      # can spot drift visually. We deliberately don't precompute a
      # ratio here: hook calls (tool-call / codex.notify.*) and OTel
      # log records use different cardinality models, so a baked-in
      # ratio would mislead during partial outages.
      - record: connector:defenseclaw_hooks:rate5m
        expr: |
          sum by (action) (
            rate(defenseclaw_audit_events_total{action=~"tool-call|tool-result|codex-hook|claude-code-hook|codex\\.notify.*"}[5m])
          )
      - record: connector:defenseclaw_hook_invocations:rate5m
        expr: |
          sum by (connector, event_type, result, reason) (
            rate(defenseclaw_connector_hook_invocations_total[5m])
          )
      - record: connector:defenseclaw_hook_latency:p95_5m
        expr: |
          histogram_quantile(0.95,
            sum by (le, connector, event_type, result) (
              rate(defenseclaw_connector_hook_latency_milliseconds_bucket[5m])
            ))
      - record: connector:defenseclaw_otel_logs:rate5m
        expr: sum by (source) (rate(defenseclaw_otel_ingest_records_total{signal="logs"}[5m]))
