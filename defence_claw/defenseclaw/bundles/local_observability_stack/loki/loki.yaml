auth_enabled: false

server:
  http_listen_port: 3100
  grpc_listen_port: 9096
  log_level: warn

common:
  path_prefix: /loki
  storage:
    filesystem:
      chunks_directory: /loki/chunks
      rules_directory: /loki/rules
  replication_factor: 1
  ring:
    kvstore:
      store: inmemory

schema_config:
  configs:
    - from: 2024-01-01
      store: tsdb
      object_store: filesystem
      schema: v13
      index:
        prefix: index_
        period: 24h

compactor:
  working_directory: /loki/compactor
  compaction_interval: 10m
  retention_enabled: false

limits_config:
  allow_structured_metadata: true
  volume_enabled: true
  reject_old_samples: false
  max_query_length: 721h
  max_global_streams_per_user: 10000
  ingestion_rate_mb: 64
  ingestion_burst_size_mb: 128
  # DefenseClaw applies bucket/route/destination redaction and the canonical
  # 4,198,400-byte projected-record ceiling before OTEL export. The local
  # Collector intentionally performs no content deletion or truncation after
  # that boundary. Give both Loki representations enough room for one maximum
  # canonical projection plus encoding overhead. An unexpected record above
  # these limits is rejected and surfaced by DefenseClawLokiIngestOverflow; it
  # is never silently accepted with a truncated body or attribute set.
  max_line_size: 5MB
  max_line_size_truncate: false
  max_structured_metadata_size: 5MB
  max_structured_metadata_entries_count: 1024

ingester:
  chunk_idle_period: 2m
  chunk_retain_period: 30s
  max_chunk_age: 30m

ruler:
  alertmanager_url: http://localhost:9093

analytics:
  reporting_enabled: false
