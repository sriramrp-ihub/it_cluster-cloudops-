# Generated by docs-site/scripts/build-policy-assets.ts during postinstall.
# Recompiled from policies/rego/*.rego whenever bundled policies change.
# Not source-controlled because it's a derived binary artifact.
*.wasm
manifest.json
