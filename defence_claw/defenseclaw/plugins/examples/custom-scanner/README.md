# Custom Scanner Plugin Example

This is a compilable Go scaffold, not a working scanner protocol
implementation. Its `main` function only prints a pointer to the engineering
guide.

See [`docs/PLUGINS.md`](../../../docs/PLUGINS.md) for implementation ownership.
