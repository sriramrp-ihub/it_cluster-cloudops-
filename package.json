{
  "name": "cloudops",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.0.0",
    "npm": ">=11.0.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*",
    "database"
  ],
  "scripts": {
    "dev": "concurrently -k -p \"[{name}]\" -n \"api,web\" -c \"magenta,blue\" \"npm run dev:api\" \"npm run dev:web\"",
    "dev:api": "npm run dev --workspace=@cloudops/api",
    "dev:web": "npm run dev --workspace=@cloudops/web",
    "build": "npm run build --workspace=@cloudops/shared && npm run build --workspace=@cloudops/database --workspace=@cloudops/security --workspace=@cloudops/events --workspace=@cloudops/capabilities && npm run build --workspace=@cloudops/audit --workspace=@cloudops/identity --workspace=@cloudops/policy --workspace=@cloudops/runtime && npm run build --workspace=@cloudops/approvals --workspace=@cloudops/onboarding --workspace=@cloudops/adapters --workspace=@cloudops/gateway && npm run build --workspace=@cloudops/tools && npm run build --workspace=@cloudops/connector && npm run build --workspace=@cloudops/api --workspace=@cloudops/web",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "vitest run",
    "test:watch": "vitest",
    "db:migrate": "tsx database/src/migrate.ts",
    "db:reset": "tsx database/src/reset.ts",
    "mock-agent:start": "tsx packages/runtime/src/mockAgentCli.ts"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/ws": "^8.18.1",
    "concurrently": "^10.0.5",
    "tsx": "^4.19.3",
    "typescript": "^5.8.2",
    "vitest": "^3.0.7",
    "ws": "^8.21.3"
  }
}
